{
  "name": "@bigraph-viz/bigraph-viz2",
  "version": "0.3.8",
  "description": "Lightweight read-only bigraph renderer",
  "type": "module",
  "main": "./dist/bigraph-viz2.esm.js",
  "files": ["dist"],
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.config.iife.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.11.0",
    "happy-dom": "^14.0.0",
    "terser": "^5.30.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.5.0"
  },
  "license": "Apache-2.0"
}
